✅ PART 1: WHATSAPP INTEGRATION (NO BACKEND NEEDED)

This sends lead details directly to your WhatsApp.

🔹 WhatsApp Button (React Component)
📌 Use this when:

You want instant parent/student contact

No database needed

Very high conversion

✅ Code (React)
const WhatsAppButton = ({ name, phone, city }) => {
  const message = `Hello LeapStart Team,%0A
I am interested in the 4-Year Engineering Program.%0A
Name: ${name}%0A
Phone: ${phone}%0A
City: ${city}`;

  return (
    <a
      href={`https://wa.me/91YOURNUMBERHERE?text=${message}`}
      target="_blank"
      rel="noopener noreferrer"
      className="bg-green-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-green-700 transition"
    >
      Chat on WhatsApp
    </a>
  );
};

export default WhatsAppButton;


👉 Replace 91YOURNUMBERHERE with your official LeapStart WhatsApp number.

🔹 WhatsApp Floating Button (Recommended)
<a
  href="https://wa.me/91YOURNUMBERHERE"
  target="_blank"
  className="fixed bottom-6 right-6 bg-green-500 text-white p-4 rounded-full shadow-lg hover:bg-green-600"
>
  WhatsApp
</a>

✅ PART 2: BACKEND FORM INTEGRATION (GOOGLE SHEETS – BEST & SIMPLE)

This stores all leads safely in Google Sheets.

🟢 OPTION A (BEST): Google Sheets + Google Apps Script
🎯 Why this is best for LeapStart

Free

No server needed

Very reliable

Easy for counselors to track leads

🔹 STEP 1: Create Google Sheet

Columns:

Name | Phone | Class | City | Date

🔹 STEP 2: Add Google Apps Script

Open Google Sheet

Extensions → Apps Script

Paste this 👇

function doPost(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = JSON.parse(e.postData.contents);

  sheet.appendRow([
    data.name,
    data.phone,
    data.className,
    data.city,
    new Date()
  ]);

  return ContentService
    .createTextOutput(JSON.stringify({ success: true }))
    .setMimeType(ContentService.MimeType.JSON);
}


Deploy → New Deployment

Type: Web App

Access: Anyone

Copy the Web App URL

🔹 STEP 3: React Form (Frontend)
import { useState } from "react";

const DemoForm = () => {
  const [form, setForm] = useState({
    name: "",
    phone: "",
    className: "",
    city: ""
  });

  const handleSubmit = async (e) => {
    e.preventDefault();

    await fetch("YOUR_GOOGLE_SCRIPT_URL", {
      method: "POST",
      body: JSON.stringify(form),
    });

    alert("Thank you! Our team will contact you shortly.");
  };

  return (
    <form onSubmit={handleSubmit} className="space-y-4 max-w-md">
      <input className="input" placeholder="Student Name"
        onChange={e => setForm({...form, name: e.target.value})} />

      <input className="input" placeholder="Phone Number"
        onChange={e => setForm({...form, phone: e.target.value})} />

      <input className="input" placeholder="Class"
        onChange={e => setForm({...form, className: e.target.value})} />

      <input className="input" placeholder="City"
        onChange={e => setForm({...form, city: e.target.value})} />

      <button className="bg-blue-600 text-white px-6 py-3 rounded-lg">
        Book Free Demo
      </button>
    </form>
  );
};

export default DemoForm;


👉 Replace YOUR_GOOGLE_SCRIPT_URL

🔹 Tailwind Input Style (Optional)
.input {
  @apply w-full p-3 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500;
}

✅ PART 3: COMBO (BEST CONVERSION STRATEGY)

After form submission:

✅ Save to Google Sheets

✅ Auto-open WhatsApp

window.open(
  `https://wa.me/91YOURNUMBERHERE?text=I submitted the LeapStart demo form`,
  "_blank"
);